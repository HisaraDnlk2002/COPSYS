// Flip this to false once your teammate's real MongoDB/JWT API is ready
// and you've confirmed the request/response shapes match ARCHITECTURE.md.
// Every file in /services checks this flag so you change it in ONE place.
export const USE_DUMMY_DATA = true;
