# Copy this file to .env and adjust if your server runs somewhere other
# than localhost:5000. Vite requires the VITE_ prefix to expose a var to
# the browser.
VITE_API_BASE_URL=http://localhost:5000/api
